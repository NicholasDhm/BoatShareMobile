import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 50,
    flex: 1,
    width: 100,
    flexDirection: "row",
  },
  text: {
    fontSize: 12,
    color: "#000",
    marginLeft: 5,
  },
});
