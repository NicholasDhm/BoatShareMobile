import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  input: {
    backgroundColor: '#fff',
    padding: 12,
    height: 42,
    borderWidth: 1,
    borderRadius: 6,
  },
  title: {
    fontSize: 16,
    paddingBottom: 4,
  },
});
