import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    height: 32,
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
  },
  weekDay: {
    fontSize: 14,
    flex: 1,
    textAlign: 'center',
  },
});
